# Backend environment variables
NODE_ENV=production
PORT=8000
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/apnacollegedemo
SESSION_SECRET=replace-with-strong-secret
JWT_SECRET=replace-with-strong-jwt-secret
JWT_EXPIRES_IN=7d
# Frontend URL (Amplify)
FRONTEND_URL=https://main.d3f2jrzjf4r80a.amplifyapp.com
# Optional cookie domain if using custom domain (omit for default domains)
# COOKIE_DOMAIN=yourdomain.com